# Random Certificate Generator

A lightweight application that generates dummy certificate data in CSV format from a Python backend and dynamically renders it onto a frontend HTML template. 

## Tech Stack
* **Backend:** Python, Flask, Flask-CORS
* **Frontend:** HTML5
